<?php

/**
 * Lombardia Informatica S.p.A.
 * OPEN 2.0
 *
 *
 * @package    lispa\amos\cwh
 * @category   CategoryName
 */

namespace lispa\amos\cwh\assets;

use yii\web\AssetBundle;

class ModuleAttivitaAsset extends AssetBundle
{
    public $sourcePath = '@vendor/lispa/amos-cwh/src/assets/web';

    public $css = [
    ];
    public $js = [
        'js/aree-interesse.js'
    ];
    public $depends = [
    ];

    /*
    //force reset cache asset (devel ONLY)
    public $publishOptions = [
        'forceCopy'=>true,
    ];
    */

    public function init()
    {
        $moduleL = \Yii::$app->getModule('layout');
        if (!empty($moduleL)) {
            $this->depends [] = 'lispa\amos\layout\assets\BaseAsset';
        } else {
            $this->depends [] = 'lispa\amos\core\views\assets\AmosCoreAsset';
        }
        $moduleTag = \Yii::$app->getModule('tag');
        if(!empty($moduleTag)){
            $this->depends [] = 'lispa\amos\tag\assets\ModuleTagFormAsset';
        }
        parent::init(); // TODO: Change the autogenerated stub
    }
}
